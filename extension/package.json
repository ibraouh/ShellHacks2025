{
  "name": "accessibility-tools-extension",
  "version": "1.0.0",
  "description": "Chrome extension with accessibility tools",
  "scripts": {
    "build": "node build-html.js",
    "dev": "npm run build && echo 'Extension ready for development'",
    "watch": "nodemon --watch html/tools --ext html --exec 'npm run build'"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["chrome-extension", "accessibility", "tools"],
  "author": "Your Team",
  "license": "MIT"
}
